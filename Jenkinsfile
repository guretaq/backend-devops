pipeline {
    agent any
    stages{
        stage("primera etapa"){
            steps{
                echo "saludo desde mi primer pipeline"
            }
        }
        stage("segunda etapa"){
            steps{
                echo "saludo desde mi segunda etapa del pipeline"
            }
        }
    }
}